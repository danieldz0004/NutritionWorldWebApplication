@model NutritionWorld.Models.IndexViewModel
@{
    ViewBag.Title = "Manage";
}

<h2>@ViewBag.Title</h2>

<p class="text-success">@ViewBag.StatusMessage</p>
<div>
    <h4>Change your account settings</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>Account:</dt>
        <dd>
            [
            @if (Model.HasPassword)
            {
                @Html.ActionLink("Change account settings", "ChangePassword")
            }
            else
            {
                @Html.ActionLink("Establish", "SetPassword")
            }
            ]
        </dd>
    </dl>
</div>
